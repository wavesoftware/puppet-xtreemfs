HOSTS:
  debian-8-x64-docker:
    roles:
      - master
    platform: debian-8-amd64
    image: debian:8
    hypervisor: docker
    docker_preserve_image: true
CONFIG:
  type: foss
  log_level: verbose
